interface:
  display_name: "Go Spec, Packages, and Secure Web"
  short_description: "Go spec, packages, and secure web defaults."
  default_prompt: "Use $go to answer Go questions with citations and apply the bundled Argon2, net/http, secure-cookie, and self-signed TLS defaults when recommending app patterns."
